"""Residual matrix A (Theorem 2.1, eq 2.1).

NOT IMPLEMENTED - SOURCE AMBIGUOUS.
The exact entry formula a_{j,k} is in eq (2.1), whose content was
stripped during extraction. No formula is invented.
"""

class ResidualMatrix:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "NOT IMPLEMENTED - SOURCE AMBIGUOUS: "
            "eq (2.1) entry formula not recovered from paper. "
            "Requires LaTeX source or direct PDF reading."
        )